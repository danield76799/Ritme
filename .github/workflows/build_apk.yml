name: Build Android APK (Ritme)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  actions: read
  checks: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.41.9'
        channel: 'stable'
        cache: true
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'
    
    - name: Get dependencies
      run: flutter pub get
    
    - name: Build unsigned APK
      run: flutter build apk --release --split-per-abi
    
    - name: Sign APKs
      env:
        KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        KEYSTORE_PASSWORD: *** secrets.KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        KEY_PASSWORD: *** secrets.KEY_PASSWORD }}
      run: |
        set -e
        
        # Decode keystore
        echo "$KEYSTORE_BASE64" | base64 -d > /tmp/keystore.jks
        
        # Sign all APKs
        for apk in build/app/outputs/flutter-apk/*-release.apk; do
          echo "Signing $apk"
          jarsigner -sigalg SHA256withRSA -digestalg SHA-256 \
            -keystore /tmp/keystore.jks \
            -storepass "$KEYSTORE_PASSWORD" \
            -keypass "$KEY_PASSWORD" \
            "$apk" "$KEY_ALIAS"
        done
        
        echo "All APKs signed successfully"
    
    - name: Upload APK Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ritme-release-apk
        path: build/app/outputs/flutter-apk/*-release.apk
    
    - name: Create Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        files: build/app/outputs/flutter-apk/*-release.apk
        tag_name: v1.2.1-build${{ github.run_number }}
        name: Ritme v1.2.1 (Build ${{ github.run_number }})
        body: |
          ## Ritme - SRT Tracker v1.2.1
          
          **Build info:**
          - Build number: ${{ github.run_number }}
          - Commit: ${{ github.sha }}
          - Type: Signed release build
          
          **Let op:** Dit is een automatische build.
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: *** secrets.GITHUB_TOKEN }}